# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

使用者只有本人——一名自认没有美学细胞的程序员。他们无法说出"为什么不好看"，但能分辨"好看/不好看"，需要工具把美学判断变成可执行的确定性规则。长期目标人群：同样没有设计背景、但需要产出前端界面的程序员。

## Product Purpose

让一个不会设计的程序员快速敲定一个项目的美学定义：**打开即看见成品，凭画面选一套方案，
（可选）改成自己的品牌色，一次复制完整内容给 AI。**

固定使用过程：`打开即见成品 → 选择喜欢的案例 → 可选调整品牌色 → 一次复制给 AI`。业务需求在
工具外与这份内容一起交给 AI。相同输入输出固定，不引入 LLM、API key 或随机生成流程。

成功标准：不用理解色彩空间、间距基准和设计策略，就能选到满意效果；接收输出的 AI 能在真实
业务页面上还原相应的视觉特征，而不只是采用相同色值。

## Positioning

确定性规则驱动的美学生成：所有生成逻辑由设计原则文档中的定理约束（OKLCH 宜居带、60-30-10
法则等），不用 LLM、不用 API key、结果可复现。核心主张：美学可以像程序一样被推导，而非靠天赋。

当前形态：**案例驱动**——七种风格的完整案例（每种都有着陆页与工作台）是"好不好看"的直观
验证载体，风格缩略图从真实案例样式生成，不用宣传图代替实际效果。

## Operating Context

本地离线使用：纯静态 HTML，双击或 `python3 -m http.server` 打开，浏览器即用，无构建步骤。
界面语言为中文。用户通过案例的直观效果判断系统好坏，专业参数（色彩策略、颜色权重、间距基准、
H-C-L 滑块）与检查信息（诊断、自检、色阶、空间阶梯、Token 表）收进折叠区，不挡预览与复制。

## Capabilities and Constraints

已实现：
- 成品前置的主流程：打开即生成标准风格完整预览，无需先上传 Logo、输入颜色或点击生成
- 七种风格阵容（标准 standard / 暖糖 soft / 流光 glass / 书卷 editorial / 褐页 sepia /
  构色 poster / 展厅 gallery），每种都有完整着陆页 + 工作台，含标题、正文、导航、按钮、
  输入、内容列表／卡片与状态
- 风格注册表单源：默认色、世界变量、作用域样式、材料色、导出文案与用途适配集中在一处，
  预览、缩略图、比较样张与导出全部由它派生
- 色彩诊断/自动校正/色阶生成（OKLCH 空间），APCA 对比度验证，CSS 变量与 Tailwind 导出
- 间距阶梯引擎（4px/8px 基准、12 级等比阶梯、语义 token）、生成后确定性自检
- 主出口「复制给 AI」：一次包含 tokens、风格规则、必须保留/可自行安排的分工、组件 CSS、
  四种页面用途做法、亮暗主题、资源与回退、交付自检清单
- 独立使用验证：只把导出的 tokens + 组件 CSS 粘进干净页面即可还原风格，页面内容与案例无关

已确认边界：
- 不提供方案保存、收藏、版本历史或撤销系统；只有一份本地状态
- 不提供随机换一批、偏好学习、专业问卷或强制初始化访谈
- 不要求用户先选择字体、圆角、阴影等独立参数才能得到完整方案
- 不新增「首次生成／修改项目」两个主输出流程；换方案后重新复制同类输出即可
- 移除「光谱 spectrum」指**可选择、可预览、可导出的生成风格**；工具外壳的纸面光谱品牌保留
- 低彩度暖褐背景（褐页）不套用强调色"脏色"判定；区分材料底色、品牌强调与功能色

约束：界面语言必须为中文（用户明确绑定）。零依赖/离线/纯静态：用户声明"可按需抛弃"，
当前保持。原有设计原则文档（color-design-principles.md）不再是不可修改的权威，可按新发现改写。

## Brand Commitments

名称 "Color Engine"（非绑定）。界面语言中文（绑定）。工具外壳的纸面光谱标识保留。
七种生成风格各自有名称与英文标识，属于风格资产而非产品品牌。无其他品牌资产。

## Evidence on Hand

- README.md（功能、使用流程与文件结构）
- STYLES.md（七种风格的提示词、使用场景、反例、三层颜色优先级与风格接口约定）
- color-design-principles.md（色彩定理集，部分内容可能被改写）
- color-engine.html + 9 个 JS 模块（当前实现）
- `assets/work-demo-01…06.svg`（展厅案例的项目自制演示图形）

## Product Principles

1. 先看成品：用户凭画面判断，不应先回答专业问题才看到结果。
2. 输入极简：输入量应少到不会设计的程序员也能完成，产出应完整到可直接使用。
3. 规则驱动、结果可复现：不用 LLM/API 也能推导出美学定义；相同输入输出固定。
4. 案例即验证：案例是美学的试金石，token 系统以"案例好不好看"为成败标准。
5. 一次复制即可交付：输出须自洽到接收方 AI 不需要用户再补组件 CSS 或解释术语。
6. 输出直接可用：导出的 token 与组件代码必须能直接粘贴进真实项目，且不依赖工具内部变量。

## Accessibility & Inclusion

工具内建 APCA 对比度验证（产品功能，非用户需求）。褐页的材料色按正文 ≥7:1、次要文字 ≥4.5:1
校验通过；所有动效配 `prefers-reduced-motion: reduce`；玻璃的 `backdrop-filter` 不支持时
退化为不透明表面且层次成立。无其他已确认的无障碍要求。
