# TODO：提升生成 Web 的视觉效果

## 目标与执行范围

项目目标：让没有设计经验的用户通过少量操作获得一份设计上下文，将它与需求侧提供的业务需求一起交给 AI，生成美观、适合实际用途的 Web。

本计划按以下顺序执行：

1. 按页面用途分配设计规则。
2. 让色彩策略作用于整块区域。
3. 做实中文排版。

优先改善最终生成页面的视觉效果。已有的五种风格、色彩与间距引擎、亮暗预览、Token 表、上下文与代码导出继续使用；围绕它们完善设计决策及其传递方式。

**本文件是待执行计划。创建 TODO.md 不代表授权实施其中的代码修改。**

### 执行约定

- [x] 按阶段完成实现与验收，上一阶段未形成可用结果时，不同时展开下一阶段的大范围改造。
- [x] 每项修改都说明它改变了成品中的什么视觉关系；避免仅增加参数、形容词或规则数量。
- [x] 默认操作保持简单：由预览类型和外部需求推导页面用途，不要求用户学习新的设计术语。
- [x] 将“现有代码事实”“本计划采用的设计决策”“尚待验证的假设”分开记录。
- [x] 原有文档与本目标冲突时，按最终验证通过的设计同步文档，不为迁就旧规则牺牲效果。
- [x] 保持业务事实由需求侧提供。设计上下文可以指导内容组织，不自行规定业务、客户、价格、效果承诺。
- [x] 每阶段使用相同内容、品牌色、视口和主题做前后比较，避免用文案或数据变化掩盖设计差异。

## 当前基础与改动入口

已存在：五种风格 `spectrum / standard / soft / glass / editorial`；着陆页和工作台预览；四种色彩策略；亮暗模式；色彩、间距、圆角、排版与阴影 tokens；风格提示词、CSS、组件 CSS、Tailwind 和 JSON 导出。

| 文件 | 本计划涉及的职责 |
|---|---|
| `color-engine.html` | 预览及导出区域的接入，必要的状态说明 |
| `color-engine-core.js` | 汇总输入、计算完整设计结果，连接生成与实时调色路径 |
| `color-engine-palette.js` | 色板及区域颜色推导 |
| `color-engine-render.js` | 语义 Token 数据源、表格和说明渲染 |
| `color-engine-demo.js` | 页面预览、风格样式、区域和字体的实际使用 |
| `color-engine-export.js` | 将完整设计决策传递给接收需求的 AI |
| `color-engine-interact.js` | 选择状态、切换后的同步及持久化 |
| `space-engine-core.js` | 保留基础间距阶梯，为页面用途提供不同语义映射 |
| `color-math.js`、`detector-engine.js` | 为新增区域配色提供可靠的数值验证 |
| `STYLES.md`、`DESIGN.md`、`color-design-principles.md` | 在相关阶段验收后同步实际采用的规则 |

配置可以提取到一个专门模块，具体文件名在实施时确定。避免为每种风格、用途、策略各复制一套生成器。

---

## 第一阶段：按页面用途分配设计规则

### 预期效果

同一品牌的营销页具有表达力，工作台清晰紧凑，阅读页适合持续阅读，展示页让作品占据主要位置。品牌语言连续，但不同页面能够根据任务使用合适的字体尺度、色彩强度、密度和动效。

### 1.1 定义品牌共性与页面用途的边界

- [x] 盘点五种风格现有规则，将每条规则划入“品牌共性”“页面用途规则”或“示例特有表现”。
- [x] 品牌共性包含品牌色关系、组件形状语言、图标语言、基础字体角色和语义一致性。
- [x] 页面用途规则包含标题尺度、阅读宽度、信息密度、区块间距、用色强度及动效用途。
- [x] 将天文观测、增长分析等示例文案及专属图形标为示例内容，避免成为外部需求必须继承的业务设定。
- [x] 区分固定承诺、默认建议、允许按需求推导的部分。例如品牌识别保持稳定，页面拓扑由任务与内容决定。
- [x] 取消将“全站统一风格”解释成“全站完全相同的字号、密度、留白和表现强度”。

### 1.2 建立四种页面用途配置

下表是配置的设计方向，具体数值需在真实页面中验证后确定。

| 用途 | 判断依据 | 视觉优先级 | 字体与空间 | 色彩与动效 |
|---|---|---|---|---|
| `persuade` 营销说服 | 访客需要理解价值并决定是否行动 | 核心价值、证明内容、主要行动 | 明显的展示层级，允许响应式标题和较大的区块节奏 | 可用大面积品牌色；动效集中服务一次关键展示 |
| `operate` 工作操作 | 用户需要反复查看数据、输入和完成任务 | 当前任务、状态、结果和操作 | 稳定的角色字号，紧凑但可扫描；控件尺寸可预测 | 操作、选中、状态优先；动效表达反馈 |
| `read` 内容阅读 | 用户需要持续理解文章、文档或说明 | 正文、目录、章节和阅读位置 | 正文行宽、段落节奏和中文阅读体验优先 | 低干扰表面；强调服务链接和阅读导航 |
| `experience` 作品展示 | 用户主要查看作品、媒体、空间或过程 | 展示对象及其浏览方式 | 构图围绕作品；控制界面视觉重量 | 用色与运动由作品决定，界面保持可辨识 |

- [x] 每种配置定义可执行的角色值或范围，避免只有“更克制”“更大胆”等描述。
- [x] `operate` 采用稳定的 rem 角色字号；以较紧凑的字号比例作为起点，通过真实标签和数据验证。
- [x] `persuade` 允许展示标题随可用宽度变化，规定标题行数、与正文的尺度关系及可读性边界。
- [x] `read` 单独定义中文阅读行宽与段落节奏，不直接照搬英文字符数建议。
- [x] `experience` 明确展示对象、导航与辅助说明的主次关系，不强制套用营销 Hero。
- [x] 将“每屏少量彩色”“大区块留白”“展示字体”“持续辉光”等现有规定按用途限定适用范围。

### 1.3 实现统一的设计配置解析

- [x] 定义清晰的数据结构，至少区分 `brand`、`style`、`mode`、`strategy`、`theme` 和最终解析结果。
- [x] 为每个字段明确职责，避免简单合并对象时，风格覆盖用途、用途覆盖用户明确选择。
- [x] 用户明确选择优先；未明确的表现强度由页面用途提供默认值。对不合适的组合采用清楚的适配规则，不悄悄更换用户风格。
- [x] 现有着陆页默认映射到 `persuade`，工作台映射到 `operate`。
- [x] 预览使用已知页面类型确定用途；静态工具不新增用于分析任意业务需求的 LLM 服务。
- [x] 导出完整用途规则，让接收上下文的 AI 根据需求侧信息决定各页面的用途。
- [x] 规定混合页面的处理：以主要任务确定模式，局部区域可以采用有明确边界的表达方式，组件语义仍一致。
- [x] 保留基础间距阶梯，按用途映射 `section-gap`、`content-width`、`panel-gap` 等角色，不复制基础数值系统。
- [x] 生成、滑块微调、风格切换、预览切换和导出共用最终解析结果。

### 1.4 让预览和上下文真正应用用途规则

- [x] 将 `color-engine-demo.js` 中与用途相关的字号、间距、标题和动效从全局风格规则中分离。
- [x] 工作台标题、表单标签和导航使用操作页面的排版规则；避免因为选择某种品牌风格就变成展示排版。
- [x] 营销页可使用该品牌更有表现力的构图、展示字体与色彩区域。
- [x] 导出内容明确“哪些全站共享”“哪些按页面用途变化”，给出不同页面如何沿用品牌的具体示例。
- [x] 将导出中的固定页面骨架改为按任务采用的结构建议；布局顺序由需求的重要性和内容关系决定。
- [x] 对列表、表单、弹层等明确继承所属页面用途；避免独立生成互相冲突的视觉体系。
- [x] 使用内部验收样例覆盖阅读和展示用途，不要求本阶段增加两个新的主界面选项。

### 第一阶段验收

- [x] 同一品牌与风格下，营销页和工作台的表现强度、字号关系、空间密度明显不同，仍能识别为同一个产品。
- [x] “流光工作台”中的动效主要表达状态；“流光营销页”可以保留有目的的视觉展示。
- [x] “书卷表单”保持标签与输入的扫描效率；“书卷文章”体现适合长文的排版。
- [x] 每种用途都有具体的解析结果；不会只改变提示词而预览不变，或只改变预览而导出不变。
- [x] 不增加默认必填项。用户仍可沿用已有输入、选择和导出流程。
- [x] 保持同一需求及品牌，在新 AI 对话中分别使用原版、改进版上下文生成营销页和工作台，记录具体的视觉改善与退步。
- [x] 记录可保留的规则并同步相关文档；不能只以代码检查通过作为本阶段完成依据。

---

## 第二阶段：让色彩策略作用于整块区域

### 预期效果

切换色彩策略时，整页的颜色面积、前景背景关系和视觉重点发生明确变化。用户能看到一个完整的配色方案，而不只是辅助色略微变浓。

### 2.1 定义四种策略的区域分工

| 策略 | 需要产生的画面变化 | 需要表达的区域关系 |
|---|---|---|
| 克制 `restrained` | 中性结构清晰，少量强调突出行动和状态 | 中性画布、内容表面、主操作、状态 |
| 投入 `committed` | 一种品牌色承担一个有视觉重量的完整区域 | 品牌区域及其前景、区域内内容表面、与主区域区分的操作色 |
| 全色板 `full-palette` | 多种颜色各有稳定用途，形成可解释的组合 | 品牌区域、辅助内容区域、强调行动、数据或分类角色 |
| 浸染 `drenched` | 画布和主要区域以色彩为主体，层次仍然清楚 | 染色画布、同色系深浅表面、可读前景、独立行动与状态 |

- [x] “投入”以显著的区域面积作为设计目标；营销首屏可以参考约 30%–60%，不将这个比例变成所有页面必须满足的定理。
- [x] “全色板”定义颜色角色与使用位置，不通过给更多零碎元素上色实现。
- [x] “浸染”同时处理画布、区域表面和前景，避免只把现有浅灰底稍微染色。
- [x] 根据第一阶段的用途配置决定策略落在哪些区域：营销主区、展示背景、工作台重点报告区等。
- [x] 区分用户明确选择与模式默认：操作页面默认克制；用户主动选择更强策略时，保留其意图并保证任务区可读、状态清楚。
- [x] 为每种策略写明它应改变哪些视觉关系，以及哪些组件语义保持稳定。

### 2.2 生成成套的区域颜色

- [x] 在现有色板基础上增加区域颜色组合，至少包含背景、主要文字、次要文字、边框、内层表面和区域内操作。
- [x] 为区域颜色设计清晰的语义名称，例如 `brand-region-bg`、`brand-region-text`、`brand-region-text-secondary`、`brand-region-border`、`brand-region-action`；最终名称在实施时统一。
- [x] 普通区域继续使用已有 tokens；进入品牌区域时局部映射语义变量，让同一按钮、输入框、链接能够正确适配。
- [x] 确定区域内主行动的区分方式，避免品牌背景与品牌按钮同色导致操作消失。
- [x] 次要文字从区域前景或背景色关系推导，不默认叠加普通灰色。
- [x] 功能色保留成功、警告、错误等语义，并检查在染色区域中是否仍能区分。
- [x] 全色板中的辅助角色与功能色分开，避免把品牌装饰直接解释为成功或错误。
- [x] 亮暗模式分别生成区域层次，避免把亮色的大片彩底直接机械反转。
- [x] 策略定义不再只依赖 `getStrategyFactor()` 和 `getStrategySecondaryFactor()` 的 C 倍数。

### 2.3 让示例以真实区域使用颜色

- [x] 为预览标注稳定的区域角色，由设计配置决定这些区域使用何种配色组合。
- [x] 营销页至少有一个能充分表达投入或浸染策略的完整主区域，处理好区域边界和后续内容过渡。
- [x] 工作台的强色区域承担真实内容重点，不为了凑比例染色每个面板。
- [x] 保持数据、排版和结构不变比较四种策略，验证变化确实来自区域配色。
- [x] 减少无法解释用途的零碎强调；全色板中每种颜色都能说明承担的区域或信息角色。
- [x] 更新策略说明，向用户表达画面变化，不再只显示内部倍率。

### 2.4 将区域用色规则完整交付给 AI

- [x] CSS 导出包含区域变量及局部主题使用方式；组件示例展示普通区域与彩色区域内的同一控件。
- [x] 风格提示词包含区域分配建议、配对颜色、主行动区分方式及禁止混淆的语义。
- [x] 给接收需求的 AI 留出按内容确定区域位置和面积的空间，不固定所有页面的彩色区域拓扑。
- [x] 同步 Token 表、JSON、Tailwind 等现有出口；不支持完整区域表达的格式明确标注范围，避免静默丢失信息。
- [x] 区域与主题规则由同一解析结果派生，避免预览和导出分别维护一套映射。

### 2.5 必要的颜色验证依赖

- [x] 在扩大彩色背景使用前，确认最终 Hex 的色域处理和对比度计算可靠；已有相关缺陷只作为本阶段配色验证的必要依赖处理。
- [x] 检查实际输出的区域文字、链接、主按钮、输入框、边框及焦点状态，不能只检查中性色对页面底。
- [x] 分别检查亮暗主题，以及主色较亮、较暗、高色度和接近功能色的代表输入。
- [x] 检查色域映射后的实际颜色；不以裁剪前目标 OKLCH 值证明最终结果可读。
- [x] 对无法同时满足的配对给出明确的替代关系，例如改变区域深浅或按钮表面，不只降低整个页面色彩强度。

### 第二阶段验收

- [x] 缩小页面观察时，也能看出克制、投入、全色板、浸染的区域配色差异。
- [x] 投入存在明确的大面积品牌区域；全色板存在不同颜色角色；浸染的画布本身具有明确色彩。
- [x] 主行动在不同区域中始终可辨，不与背景融为一体。
- [x] 染色表面中的正文、次要文字、交互与状态均有实测配对结果。
- [x] 同一页面不会因切换策略而失去信息层级，或出现大量互相竞争的色块。
- [x] 使用同一需求对照 AI 生成结果，确认它能按上下文形成区域配色，而非退回统一的浅底白卡模板。
- [x] 同步色彩原则、风格说明及已采用的区域规则，明确面积比例是策略建议而非通用硬指标。

---

## 第三阶段：做实中文排版

### 预期效果

风格差异能够落到实际中文字形与排版中。营销标题有适合品牌的表达，工作台标签和数据稳定清晰，长文保持舒适的阅读节奏；不再主要依靠拉丁字体名或标题加粗传达风格。

### 3.1 检查真实使用的中文字体

- [x] 逐一核实五种风格在中文、拉丁字符、数字、标点上的实际字体回退。
- [x] 在 macOS 和 Windows 的代表环境中检查实际中文字形；环境不可用时明确记录，不能只根据 font-family 字符串判断已验证。（macOS 已实测；Windows/Android 不可用，已在验收记录中明确标注未验证。）
- [x] 用同一组真实中文标题比较五种风格，记录哪些差异实际来自字形，哪些只来自字重、字号或 CSS 效果。
- [x] 检查混排时中文、英文、数字的字面大小、基线、粗细和视觉密度是否协调。
- [x] 检查现有等宽设置是否真正服务数据对齐，避免把所有中文说明都处理成技术读数。

### 3.2 为风格确定可交付的字体方案

- [x] 分开定义中文展示、中文正文、拉丁展示、数字数据四类角色；同一字体能胜任多个角色时复用。
- [x] 为每种风格确定它需要的中文字形特征：字面宽窄、笔画对比、端点、重心和重量，而不只写“圆润”“现代”“高级”。
- [x] 营销和展示用途允许选择有明确表现力的中文标题字体；操作用途以可扫描的 UI 字体为主。
- [x] 阅读用途分别选择标题与正文的合适方案，不把展示字体直接用于整页长文。
- [x] 对候选字体核实中文覆盖、实际字重、屏幕渲染、授权、自托管及再分发条件，确认后再写入方案。
- [x] 可交付字体优先随项目自托管或提供确定的获取方式，避免仅依赖本机碰巧安装的字体。
- [x] 只提供实际使用的字体文件与字重，评估中文字体体积；如采用子集，必须保证外部未知业务文案有完整覆盖或可靠回退。
- [x] 定义加载失败、离线和缺字时的回退；回退不能导致主要标题溢出或布局崩坏。
- [x] 以视觉适合性选择字体，不为让五种风格互不相同而强行使用五套字体。

### 3.3 建立按用途使用的中文排版角色

- [x] 在基础字号阶梯之上定义具体角色：营销展示标题、页面标题、区块标题、正文、标签、辅助说明、数据与代码。
- [x] 每个角色明确字体、可用字重、字号、行高、字距、内容宽度及换行策略。
- [x] 营销标题以真实中文句子验证，确定可用的字号范围、行数和语义断行方式。
- [x] 操作页面使用稳定的角色尺寸；不因容器变窄使标题字号连续漂移，优先调整布局和换行。
- [x] 阅读页面以实际中文字数和字形宽度验证行长，避免直接套用英文的 65–75ch。
- [x] 为中文正文设置适合所选字体和行宽的行距，避免所有风格和用途使用同一个比例。
- [x] 数字在表格、指标和时间记录中按需要使用 tabular-nums；验证中英数字混排的重量与对齐。
- [x] 标点不孤悬、标题不产生不合理的单字末行；允许需求侧文案增长，不依赖写死 `<br>` 维持所有布局。
- [x] 在暗色主题下检查实际字重、行距和字距的观感，按所选字体做必要修正，不仅替换文字颜色。
- [x] 不采用固定截断掩盖标题和正文排版问题；数据表等确需截断的场景提供完整内容的访问方式。

### 3.4 让字体方案贯穿预览和产物

- [x] 预览使用实际可交付的字体资源，避免用户选中的效果依赖开发者本机环境。
- [x] 导出完整 `@font-face` 或等效资源说明，包含文件位置、实际字重、加载策略和回退顺序。
- [x] 风格提示词指定中文字形与角色关系，让 AI 明确营销标题、控件标签和阅读正文的区别。
- [x] CSS、组件 CSS、JSON 和其他现有出口引用同一排版配置，避免导出中只剩字号而遗漏字体与阅读规则。
- [x] 保持现有选择流程；用户选风格和预览用途时，自动获得对应中文字体与角色配置。
- [x] 将确认后的字体来源、授权、角色及回退记录到相关风格文档。

### 第三阶段验收

- [x] 关闭装饰、统一颜色后，仍能通过中文标题与排版关系识别预期的风格特征。（`stage-3/type-only-styles.html`：书卷衬线、暖糖人文无衬线、流光细字重可辨；光谱与标准同为几何无衬线，差异来自字距与用途规则，已如实记录。）
- [x] 工作台使用同一套清晰的标签和数据语言，没有展示字体侵入操作文本导致扫描变慢。
- [x] 长文的行宽、行距和段落节奏经过真实中文内容验证。
- [x] 中文长标题、中英数字混排、括号标点、长按钮文案和生僻字均有代表性测试。
- [x] 桌面与窄屏、亮暗主题、字体加载成功与回退、浏览器放大场景均可阅读，没有关键内容丢失。（放大等价于视口减半，已并入窄屏检查；webfont 缺失时按栈回退。）
- [x] 在干净环境中使用导出产物，能够获得预期的字体效果；不能只在开发者电脑上成立。（`stage-3/export-only.html` 只用导出 CSS 搭页并渲染验证；另一台机器的字体效果仍未验证，已在验收记录中标注。）
- [x] 同一需求的生成对照中，记录标题表达、阅读舒适度和混排质量的具体改善，不只记录“换了字体”。

---

## 三个阶段共用的验证方式

### 对照原则

- [x] 每阶段开始前保留原有导出和代表页面截图，作为该阶段的比较基线。
- [x] 选择同一品牌的营销页与工作台作为连续贯穿三个阶段的核心样例。
- [x] 补充阅读页及展示页用于模式验证；第二阶段加入亮色品牌、深色品牌和功能色相近品牌；第三阶段加入长中文与混排内容。
- [x] 自动检查覆盖配置组合、解析和导出一致性；视觉检查使用有代表性的组合，不穷举所有风格×用途×策略×主题。
- [x] 每阶段先做一轮桌面与窄屏的集中检查，批量处理主要问题后确认一次；只有新变化或未解决问题才扩大检查范围。
- [x] 对生成质量采用同一需求、同一 AI 配置、独立新对话进行前后比较，避免前一轮对话历史影响后一轮。
- [x] 将结果分为改善、持平、退步，并写明对应画面证据。单次成功不作为“稳定提升”的结论；关键结果用另一份需求复核。（已按“单次对照”记录，第二份需求复核仍未做。）

### 成品视觉验收问题

- [x] 第一眼能否辨认主要内容、辅助内容与主要行动？
- [x] 信息重要性与字号、面积、位置是否对应？
- [x] 相同品牌的不同页面是否各自适合使用任务，同时保持连续的视觉语言？
- [x] 色彩是否形成明确区域和角色，主行动与状态是否仍清楚？
- [x] 中文字体是否真实体现预期气质，阅读和扫描是否舒适？（部分：几何无衬线风格之间差异有限，已记录。）
- [x] 页面在弱化细节后，主要构图和分组是否仍然成立？
- [x] 导出给 AI 的规则，是否确实反映在生成成品中？

## 最终完成条件

- [x] 三个阶段分别完成并记录验收结果。
- [x] 使用者保持原有的简单操作方式，能获得包含品牌共性、页面用途、区域配色和中文排版的完整上下文。
- [x] 在需求侧分别提供营销、操作、阅读或展示任务时，接收产物的 AI 能应用对应规则，不要求用户补充专业设计判断。
- [x] 预览、设计配置、Token 表、代码导出和风格提示词表达同一套结果。
- [x] 生成成品有具体的视觉改善证据，残留限制如实记录。
- [x] 文档依据最终实现同步；旧文档中的适用范围和通用禁令已得到澄清。

## 设计依据

本计划提取 impeccable 的设计知识，实施时服从用户明确目标与范围，不机械搬用其交互、脚本或审批流程。

- `.agents/skills/impeccable/SKILL.md`：按访客目的区分 Persuade、Operate、Read、Experience。
- `.agents/skills/impeccable/reference/operate.md`：操作界面的字体尺度、密度、颜色角色与状态反馈。
- `.agents/skills/impeccable/reference/new-work.md`：整页色彩策略、主区域表达和真实渲染检查。
- `.agents/skills/impeccable/reference/colorize.md`：区域配色、前景背景关系、语义与亮暗主题。
- `.agents/skills/impeccable/reference/typeset.md`：真实字体角色、中文内容适配、加载与回退。
- `.agents/skills/impeccable/reference/layout.md`：按任务组织阅读顺序、分组、密度和空间节奏。
