# Color Engine 风格指南

> 给没有美学细胞的程序员的「美学使用手册」：同一组 CSS tokens 如何在 5 种风格下呈现，
> 以及如何把「风格」翻译成可复用的提示词。打开工具第 9 区「组件案例」的风格切换，
> 可以直观对比每种风格的呈现。

---

## 一、调整前后：都做了什么（简史）

### 第一轮 · 架构与规则（正确性）

| 改动 | 为什么 |
|---|---|
| 修复实时调色 3 个 bug（高亮失效 / L 越界破坏色阶 / 分析区误写） | 交互链路正确 |
| `computeSystem` 唯一管线，generate 与 retweak 共用 | 消除两份拷贝，改一处生效 |
| Token 单一数据源（`buildTokenMap`），导出/对照表/Demo 三处同源 | 修掉了三份映射互相矛盾（禁用态文字曾映射到近白色） |
| 对比度三级层级：正文 75 / 次要 60 / 弱化 45 | 之前次要文字和正文同标准，页面没有层级——层级是"被设计过"的第一信号 |
| 中性色品牌色温贯穿全阶 | 之前文字区强制 C=0，整页"灰得没有性格" |

### 第二轮 · 美学杠杆（tokens 补全）

| 改动 | 为什么 |
|---|---|
| 排版 tokens（7 级字号 + 行高 + 字重） | 好看的 80% 来自字号对比，色彩只占 20% |
| 灰画布 + 白卡表面模型 | 白底+白卡永远发平；深度靠「白 vs 灰 + 阴影」 |
| accent-2 渐变搭档 / 品牌渐变 / 阴影色 / 圆角 / 浮层表面 tokens | 「看起来贵」的作弊码：彩色阴影与渐变 |
| Demo 从组件堆叠改为完整页面 | 组件库文档页永远不会好看，好看需要页面结构 |

### 第三轮 · 根因 Bug + 风格系统

| 改动 | 为什么 |
|---|---|
| **修复「间距 token 未注入 live CSS」根因 bug** | `generated-tokens` 只注入了颜色 token，`var(--space-*)` 全部解析为 0——所有 demo 的间距曾经塌成 0，这是此前"挤"的最深层原因 |
| 全屏预览（克隆到覆盖层 / 主题双向同步 / ESC 退出 / 内容列加宽） | 让示例占满整个屏幕验证 |
| 世界变量层 + 5 风格系统（本轮新增） | 同一组 tokens 的不同视觉世界 |

---

## 二、风格体系总览

| 风格 | 一句话 | 圆角 | 阴影 | 字体声部 | 边框 | 气质 |
|---|---|---|---|---|---|---|
| **光谱** spectrum | 纸面工业 · 天文台控制台 | 3px | 发丝偏移 + 强调微光 | Avenir Next / Bahnschrift + 系统等宽（数据） | 1px 发丝线 | 冷、可信、界面退后 |
| **暖糖** soft | 消费级亲切 | 14–20px | 柔和多层（模糊 10–30px） | SF Pro Rounded / Segoe UI Variable（字距放宽） | 浅发丝 | 暖、安全、好接近 |
| **流光** glass | AI 科技辉光 | 10–16px | 1px 描边 + 彩色辉光 | Helvetica Neue / Segoe UI（字距收紧） | 1px 半透明 | 未来、发光 |
| **书卷** editorial | 编辑部与杂志 | 2–4px | 无阴影 | 衬线（宋体 / STSong / Georgia） | 1px 细线 | 克制、慢、有分量 |
| **标准** standard | 现代 SaaS 通用 | 8–12px | 柔和双层 | 系统无衬线 | 1px 细边框 | 干净、可信、不抢戏 |

五个风格共享：同一份 HTML 结构、同一组色彩/间距/字号/阴影 tokens、亮暗双主题。切换只换一层「世界变量」（圆角、阴影、字体、边框、状态点形态）。字体全部使用系统原生栈（零依赖、离线一致）：展示声部靠 macOS/Windows 双栈区分（Avenir Next / Bahnschrift / SF Pro Rounded / Helvetica Neue / 宋体），数据一律系统等宽（SF Mono / Cascadia Code / Consolas）+ tabular-nums。

---

## 三、每个风格：可复用提示词 + 使用场景

以下每段提示词可直接复制——粘贴给 AI 作为设计 brief，或作为自己项目的风格需求描述。

### 1. 光谱世界（Spectrum）

```
风格：光谱世界（纸面工业）
- 表面：纸白底 + 1px 发丝线边框 + 微偏移阴影；3px 小倒角，禁用圆角胶囊
- 字体：展示用几何无衬线（Avenir Next / Bahnschrift 系统栈），数据/时间/编号用等宽字体（SF Mono / Cascadia Code 系统栈），
  数字一律 tabular；别让同一字重从头走到尾
- 强调色：只出现在语义位置（主按钮 / 链接 / 选中态 / 状态点），中性色承担全部结构
- 装饰：无渐变、无光斑、无 emoji；图形用统一 1.8 描边的 SVG 线稿
- 气质：像天文台控制台、实验室仪器——数据可信，界面退后
```
**使用场景**：开发者工具、数据/运维平台、交易终端、专业工作台、技术文档。
**反例**：需要亲和力的 C 端消费页（会显得冷硬、拒人）。

### 2. 暖糖（Soft）

```
风格：暖糖（消费级圆润）
- 表面：大圆角（14–20px）+ 柔和多层阴影（模糊 10–30px、透明度低）+ 浅色细边框；状态点圆形
- 字体：圆润无衬线（SF Pro Rounded / Segoe UI Variable 系统栈），字距可放宽，少用等宽字体
- 强调色：可以更活泼——按钮、徽章、图标底都能用，但一屏仍不超过 3 处
- 微交互：hover 上浮 1px，按压下沉，过渡 150ms
- 气质：亲切、安全、值得信任，像健康/教育类 App
```
**使用场景**：C 端产品、教育、健康、电商、社区。
**反例**：数据密集的管理后台、严肃企业工具（大圆角会显得不专业）。

### 3. 流光（Glass）

```
风格：流光（玻璃科技）
- 表面：半透明玻璃面板（backdrop-filter blur 14–18px）+ 1px 半透明描边 + 大圆角（10–16px）
- 光效：主 CTA 带强调色辉光（0 0 0 1px 描边 + 8–26px 彩色阴影）；大区块顶部可加径向光斑
- 字体：现代几何无衬线（Helvetica Neue / Segoe UI 系统栈），标题字距略收紧
- 气质：AI 产品、未来感、发光但不刺眼
```
**使用场景**：AI 产品、SaaS 官网、金融科技、游戏。
**反例**：长文阅读产品（玻璃和光效干扰阅读）。

### 4. 书卷（Editorial）

```
风格：书卷（编辑内容）
- 表面：纯色底 + 1px 细线分隔，几乎不用阴影；圆角 2–4px 或直角
- 字体：衬线显示字体（中文宋体 / 英文 Georgia 类；Windows 回退 STSong / SimSun），标题不收紧字距，正文行高 1.7
- 强调色：全场最多 1 处（链接或 CTA 二选一）
- 留白：区块间距 ≥ 80px，标题与正文间 8–12px；宁可空，不可挤
- 气质：杂志、出版社、编辑部
```
**使用场景**：内容平台、媒体、品牌官网、文档站、博客。
**反例**：数据面板、需要一眼扫完的操作界面（衬线加大留白会拖慢扫描）。

### 5. 标准（Standard）

```
风格：标准（现代 SaaS）
- 表面：浅底 + 白卡浮起（适中圆角 8–12px + 柔和双层阴影），1px 细边框
- 字体：系统无衬线（Apple 栈），标题加粗 + 正文常规，数字 tabular；不引入特殊字体
- 强调色：标准用法——主按钮实色、链接、选中态、焦点环，一屏 ≤ 3 处
- 交互：hover 轻微上浮 + 阴影加深，active 内凹，过渡 150ms
- 装饰：无网格、无印章、无光斑、无渐变文字；图标用统一描边 SVG
- 气质：大多数成熟 SaaS 的通用面貌——干净、可信、不抢戏
```
**使用场景**：**默认首选**——SaaS 后台、B2B 官网、管理工具、内部系统、通用 Web 应用。拿不准选什么风格时选它。
**反例**：需要强品牌记忆点的营销页（标准风格不制造记忆点，但也不会出错）。

> 定位：风格体系里的「中间值」。光谱偏工业、暖糖偏消费、流光偏科技、书卷偏文艺，
> 标准就是那个什么都不偏的选项——大多数真实产品长这样。

---

## 四、通用提示词模板（tokens + 风格 + 场景）

> 工具内已内置此模板：导出区选择「**风格提示词**」标签，会自动把当前生成的 tokens 具体值
> + 当前风格提示词 + 页面场景 + 约束拼成完整 prompt，一键复制丢给 AI 即可。
> 切换风格 / 着陆页 / 工作台后导出内容自动跟随。

给 AI 描述一个页面时，用这个三明治结构——缺了任何一层，结果都会跑偏：

```
【项目】<一句话说明产品是做什么的>
【tokens】已有一组设计 token（色彩/间距/圆角/字号/阴影），全部以 CSS 变量提供，
          只能使用这些变量，不得自造颜色
【风格】<从上方五种风格中选一种，粘贴对应提示词>
【场景】<页面类型：着陆页 / 工作台 / 表单 / 列表页 / 设置页…>
【约束】不用渐变文字；不用 emoji 做图标（用统一描边 SVG）；阴影必须有偏移+模糊；
        一屏一个主角元素
```

---

## 五、五条设计约定（让页面不丑的最低标准）

1. **强调是例外**：一屏只给主按钮 / 链接 / 选中态；彩色只出现在语义位置，中性承担全部结构
2. **字体有声音**：展示字体 + 正文成对；数据用等宽 tabular 数字，别让同一字重从头走到尾
3. **组内紧、组间松**：标题组内 8px，卡片内 24px，区块间 ≥ 48px；宁可空，不可挤
4. **深度靠阴影不靠色**：阴影带偏移 + 模糊；不叠圆角胶囊、不用渐变文字
5. **禁止 AI 俗套**：图标卡平铺、眉题 eyebrow、灰底灰字、卡片套卡片——一行都不留

---

## 附：选择风格的心法（30 秒决策）

- 产品要**显得可信/专业**（工具、数据、B2B）→ 光谱
- 产品要**显得亲切/好卖**（C 端、消费）→ 暖糖
- 产品要**显得先进/AI** → 流光
- 产品要**显得有文化/内容** → 书卷
- **拿不准选什么** → 标准（不会错，也不会出彩）

---

## 七、更好看升级：候选提示词（待定是否并入「风格提示词」导出）

> **候选状态：** 以下提示词是本轮「更好看」升级的实现手法，已实现在组件案例里，
> **尚未**写入第 10 区「风格提示词」导出（`STYLE_PROMPTS`）。看过实际效果后再决定是否并入。
> 通用原则不变：每屏只做一个有作者意图的动效时刻；所有动画配 `prefers-reduced-motion` 关闭；
> 弱化色（muted）只用于占位/禁用，真实内容小标签一律用次要色（≥4.5:1）。

| 风格 | 候选魔法 | 手法一句话 |
|---|---|---|
| 光谱 | **图版编号** | 区块标题前置 mono 图版编号（PLATE 01 / 02…），0.6rem + 0.18em 字距——科学图版语言，只在本世界显示 |
| 暖糖 | **列表卡片化** | 无卡的区块（今夜窗口列表）包成 raised 卡片：大圆角 + 柔和多层阴影 + 去掉发丝竖线，让柔影有落点 |
| 流光 | **玻璃高光** | 玻璃面板加 inset 顶部高光（`0 1px 0 rgba(255,255,255,.26)`）制造玻璃边缘厚度；顶栏半透明玻璃化（52% + blur 12px） |
| 书卷 | **衬线数据声部** | 数据/时间/编号的等宽声部整体换成衬线（宋体/Georgia）——杂志读数是文字不是仪表；笔墨下划线扩展到导航链接 |
| 标准 | **克制清单** | 标准的"更好看"= 不加东西：只保留单一进场时刻，其余区块零动效零装饰——克制即签名 |

### 可复用提示词片段（每段可直接粘贴进风格 prompt 的补充）

```
【光谱 · 图版编号】区块标题上方加 mono 微标注图版编号（PLATE 01、02…）：0.6rem、
0.18em 字距、次要色；编号是仪器语言，只在工业/科学类世界使用，禁止当装饰
```
```
【暖糖 · 列表卡片化】页面里没有卡片的列表区块（时间线/窗口/日程）包成一张卡片：
surface-raised 底 + 14–20px 圆角 + 柔和多层阴影（0 12px 32px -14px，20–22% 文字色）；
去掉区块原有的发丝竖线，圆点状态保留——让"大圆角柔影"在每一屏都有落点
```
```
【流光 · 玻璃高光】半透明玻璃面板加 inset 顶部高光（inset 0 1px 0 rgba(255,255,255,.26)），
模拟玻璃边缘厚度；横贯型顶栏同样半透明化（52% 表面色 + blur 12px）——高光让玻璃"有厚度"，
不再像一层雾
```
```
【书卷 · 衬线数据声部】把数据/时间/编号用的等宽字体整体换成衬线（宋体 / Georgia 类）——
杂志里的数字是文字不是仪表读数；笔墨下划线（scaleX 生长）从链接扩展到导航项，
hover 才出现，尊重 reduced-motion
```
```
【标准 · 克制清单】标准的升级清单是"不加"：全页唯一进场时刻（hero 淡入上浮一次）；
其余区块零动效、零装饰、零新字体；hover 只做上浮 + 阴影加深——克制即签名，
任何一条"更酷"的添加都会破坏它
```

### 本轮同时修复的通用缺陷（也建议并入约束层）

- **文字层级纪律**：弱化色（muted）只用于占位符与禁用态；真实内容的小标签（时间、计数、图注、页脚）一律用次要色——最小号字配最弱对比是"没设计过"的信号
- **强调色文本对比**：小号链接文字用强调色阶最深档（accent-active）而非主强调色，保证 AA；彩色文字用"色阶更深的档"
- **进场时刻归属**：棱镜五带进场只属于光谱世界；其余世界静态——每个世界只能有一个作者时刻
- **窄屏顶栏折行**：topbar 在 ≤560px 折行，按钮不再溢出卡片（溢出是移动端第一丑）

### 内容世界化（候选 · 已实现于全部五个世界，等确认后并入导出）

> 五个风格共用同一套内容时，差异上限是"皮肤级"。要让差异变成"天壤之别"，
> 内容要随世界更换——同一套结构骨架（.ce-topbar/.ce-hero/.ce-plate/.ce-split/.ce-app…）
> + 同一组 tokens，每个世界换叙事、换图形语言、换区块编排。
> 已实现：光谱=天文台、标准=增长分析、暖糖=睡眠放松、流光=AI 排程、书卷=城市月刊。

```
【标准 · 内容世界化】SaaS 数据后台叙事：
- 品牌 PULSE；hero 讲"把转化算清楚"；主图=折线趋势图（发丝网格线 + accent 折线 + 目标虚线）
- 编排：hero → 指标条（三个大数字卡：月活/转化率/客单价 + 环比 delta）→ 报表表格 → 数据源/共享分栏
- 工作台：报表列表 / 转化趋势 / 关键指标
```
```
【暖糖 · 内容世界化】消费健康 App 叙事：
- 品牌「舒心」；hero 讲"睡个好觉"；主图=呼吸圆环（accent 描边 dasharray 进度环 + 4-7-8 数字 + 节拍圆点）
- 编排：hero → 今晚计划时间线（步骤 + 时长 + 状态）→ 课程列表/打卡分栏
- 工作台：今日计划 / 呼吸练习 / 本周数据
```
```
【书卷 · 内容世界化】杂志专题叙事：
- 品牌「知卷」；hero 是刊头大字报（大衬线刊号 07 + 发丝线 + 卷期信息），副文即杂志引言
- 编排：hero → 本期要点（篇目 + 作者 + 页码）→ 引文块（.ce-quote：衬线引文 + mono 出处）→ 专栏/目录分栏
- 工作台：稿件队列 / 版面样张 / 排期
```
```
【流光 · 内容世界化】AI 产品叙事（样板）：
- 品牌 NEXUS；hero 讲"模型自动接管数据流"；主图=数据管线节点图（S1/S2/S3→ROUTER→OUT，描边 1.2–1.5px）
- 编排：hero → 运行中的智能体（名称 + 负载条 + 延迟 + 状态）→ 数据源/任务队列分栏
- 工作台：智能体列表 / 实时管线图 / 实时指标
```
