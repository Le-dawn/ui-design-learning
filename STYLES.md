# Color Engine 风格指南

> 给没有美学细胞的程序员的「美学使用手册」：同一组 CSS tokens 如何在 7 种风格下呈现，
> 以及如何把「风格」翻译成可复用的提示词。打开工具首页的风格选择区，缩略图直接从
> 真实案例样式生成，选谁就在主预览看谁。

---

## 一、调整前后：都做了什么（简史）

### 第一轮 · 架构与规则（正确性）

| 改动 | 为什么 |
|---|---|
| 修复实时调色 3 个 bug（高亮失效 / L 越界破坏色阶 / 分析区误写） | 交互链路正确 |
| `computeSystem` 唯一管线，generate 与 retweak 共用 | 消除两份拷贝，改一处生效 |
| Token 单一数据源（`buildTokenMap`），导出/对照表/Demo 三处同源 | 修掉了三份映射互相矛盾（禁用态文字曾映射到近白色） |
| 对比度三级层级：正文 75 / 次要 60 / 弱化 45 | 之前次要文字和正文同标准，页面没有层级——层级是"被设计过"的第一信号 |
| 中性色品牌色温贯穿全阶 | 之前文字区强制 C=0，整页"灰得没有性格" |

### 第二轮 · 美学杠杆（tokens 补全）

| 改动 | 为什么 |
|---|---|
| 排版 tokens（7 级字号 + 行高 + 字重） | 好看的 80% 来自字号对比，色彩只占 20% |
| 灰画布 + 白卡表面模型 | 白底+白卡永远发平；深度靠「白 vs 灰 + 阴影」 |
| accent-2 渐变搭档 / 品牌渐变 / 阴影色 / 圆角 / 浮层表面 tokens | 「看起来贵」的作弊码：彩色阴影与渐变 |
| Demo 从组件堆叠改为完整页面 | 组件库文档页永远不会好看，好看需要页面结构 |

### 第三轮 · 根因 Bug + 风格系统

| 改动 | 为什么 |
|---|---|
| **修复「间距 token 未注入 live CSS」根因 bug** | `generated-tokens` 只注入了颜色 token，`var(--space-*)` 全部解析为 0——所有 demo 的间距曾经塌成 0，这是此前"挤"的最深层原因 |
| 全屏预览（克隆到覆盖层 / 主题双向同步 / ESC 退出 / 内容列加宽） | 让示例占满整个屏幕验证 |
| 世界变量层 + 风格系统 | 同一组 tokens 的不同视觉世界 |

### 第四轮 · 先看成品，再按需调整（当前）

| 改动 | 为什么 |
|---|---|
| 打开即见成品：风格选择区 + 完整案例预览 + 「复制给 AI」 | 用户能凭画面判断好坏，但不想先回答专业问题才看到结果 |
| 移除生成风格 **光谱 spectrum**，默认与未知值统一回退 **标准** | 案例阵容收敛；旧单份状态里的 `spectrum` 自动映射到 `standard`，不报错 |
| **重做流光 glass** | 旧版多色径向光斑叠加让页面发灰、文字背景不稳；新版先建立干净底色与清晰层次，再加一处主光效 |
| 新增 **褐页 sepia / 构色 poster / 展厅 gallery** | 补上用户明确要的褐色书卷方向，以及大胆平面与作品主导两个缺口 |
| Logo / 品牌色 / 色彩策略 / 间距基准 / H-C-L 滑块移入「详细调整」 | 参数退出必经流程，无 Logo 无品牌色也能直接用 |
| 「复制给 AI」成为主出口；CSS 变量 / 组件 CSS / Tailwind / JSON 移入高级导出 | 一次复制包含选中方案的完整上下文，不必逐项收集 |

> 说明：移除「光谱」指**可选择、可预览、可导出的生成风格**。工具外壳的纸面光谱品牌
> （顶栏标识、页脚说明）保留，不属于生成风格阵容。

---

## 二、风格体系总览

| 风格 | 一句话 | 圆角 | 阴影 | 字体声部 | 边框 | 气质 |
|---|---|---|---|---|---|---|
| **标准** standard | 现代 SaaS 通用 | 8–12px | 柔和双层 | 系统无衬线 | 1px 细边框 | 干净、可信、不抢戏 |
| **暖糖** soft | 消费级亲切 | 14–20px | 柔和多层（模糊 10–30px） | SF Pro Rounded / Segoe UI Variable（字距放宽） | 浅发丝 | 暖、安全、好接近 |
| **流光** glass | 清透玻璃 · 单一光效 | 10–16px | 1px 描边 + 局部玻璃厚度 | Helvetica Neue / Segoe UI（字距收紧） | 1px 半透明 | 未来、清透、有明确主角 |
| **书卷** editorial | 编辑部与杂志 | 2–4px | 无阴影 | 衬线（宋体 / STSong / Georgia） | 1px 细线 | 克制、慢、有分量 |
| **褐页** sepia | 暖褐纸面 · 旧书与档案 | 2–6px | 几乎无阴影，靠边界与轻层次 | 中文衬线标题 + 可读正文 | 1px 暖褐细线 | 旧、暖、有年代感 |
| **构色** poster | 鲜明色块 · 海报式排版 | 0px（直角） | 无阴影，改用硬边错位 | 几何无衬线，大字重对比 | 2px 实线 | 大声、平面、有节奏 |
| **展厅** gallery | 作品主导 · 界面退后 | 2px | 无阴影 | 系统无衬线，标题中等字重 | 1px 细线 | 安静、让位给作品 |

七种风格共享：同一份 HTML 结构骨架、同一组色彩/间距/字号/圆角/阴影 tokens、亮暗双主题。
切换只换一层「世界变量」（圆角、阴影、字体、边框、状态点形态）与各自的**作用域样式**
（`.ce-style-*`），不写全局选择器，互不干扰。

字体全部使用系统原生栈（零依赖、离线一致）：展示声部靠 macOS/Windows 双栈区分，
数据一律系统等宽（SF Mono / Cascadia Code / Consolas）+ `tabular-nums`。

### 品牌色 / 材料色 / 用途尺度：三层优先级

同一份方案里，颜色来自三个不同层级，输出与预览遵循同一条规则：

1. **风格材料色**（最高优先，仅材料主导的风格有）：如褐页的 `--ce-page` / `--ce-rule` /
   `--ce-ink`，是"纸"本身。它不受色彩策略缩放影响，**低彩度暖褐不会被当成脏色自动纠正**。
2. **品牌强调色**（用户显式设置时优先于风格默认色）：只落在语义位置——主按钮、链接、
   选中态、状态点、局部标记。自定义颜色用于适量操作与标记，不把材料底完全染掉。
3. **用途尺度**（色彩策略 ×0.5 / ×1.0 / ×1.5 / ×2.0）：只缩放**背景区的色温峰值**与
   辅助色强度，用来控制整页品牌色浓度，不改材料色与功能色。

---

## 三、每个风格：可复用提示词 + 使用场景

以下每段提示词可直接复制——粘贴给 AI 作为设计 brief，或作为自己项目的风格需求描述。
工具内「复制给 AI」会把这些内容与当前 tokens、组件 CSS、用途规则拼成一份完整输出。

### 1. 标准（Standard）

```
风格：标准（现代 SaaS）
- 表面：浅底 + 白卡浮起（适中圆角 8–12px + 柔和双层阴影），1px 细边框
- 字体：系统无衬线（Apple 栈），标题加粗 + 正文常规，数字 tabular；不引入特殊字体
- 强调色：标准用法——主按钮实色、链接、选中态、焦点环，一屏 ≤ 3 处
- 交互：hover 轻微上浮 + 阴影加深，active 内凹，过渡 150ms
- 装饰：无网格、无印章、无光斑、无渐变文字；图标用统一描边 SVG
- 气质：大多数成熟 SaaS 的通用面貌——干净、可信、不抢戏
```
**使用场景**：**默认首选**——SaaS 后台、B2B 官网、管理工具、内部系统、通用 Web 应用。拿不准选什么风格时选它。
**反例**：需要强品牌记忆点的营销页（标准风格不制造记忆点，但也不会出错）。

### 2. 暖糖（Soft）

```
风格：暖糖（消费级圆润）
- 表面：大圆角（14–20px）+ 柔和多层阴影（模糊 10–30px、透明度低）+ 浅色细边框；状态点圆形
- 字体：圆润无衬线（SF Pro Rounded / Segoe UI Variable 系统栈），字距可放宽，少用等宽字体
- 强调色：可以更活泼——按钮、徽章、图标底都能用，但一屏仍不超过 3 处
- 微交互：hover 上浮 1px，按压下沉，过渡 150ms
- 气质：亲切、安全、值得信任，像健康/教育类 App
```
**使用场景**：C 端产品、教育、健康、电商、社区。
**反例**：数据密集的管理后台、严肃企业工具（大圆角会显得不专业）。

### 3. 流光（Glass）

```
风格：流光（清透玻璃 · 单一光效）
- 先干净再发光：全页一个不透明基底，层级由字号、字重、间距和边框建立；
  把渐变与光效全部去掉后，页面层次仍然成立——这是验收前提
- 一个主光效区：全页只允许一处光效主角，且只用一个色相（强调色的淡化档），
  不做多色径向叠加；不让每个区块、按钮、卡片同时发光
- 玻璃是局部构件：只用于顶栏导航、主视觉卡与一个重点展示面板；
  backdrop-filter blur 12–16px + 1px 半透明描边 + inset 顶部高光，
  靠前后遮挡与透光边缘体现厚度，而不是把所有面板做成灰色半透明雾
- 稳定表面：表格、输入、长文本、工作台主要操作区一律用不透明表面
- 动效：全页只有一个服务主视觉的运动（管线描线）；取消无目的的持续按钮呼吸辉光；
  所有动画配 prefers-reduced-motion: reduce
- 字体：现代几何无衬线（Helvetica Neue / Segoe UI 系统栈），标题字距略收紧
- 暗色：深底 + 清晰层次，不是亮色的机械反转，也不堆多色霓虹
- 气质：AI 产品、未来感、清透但不脏
```
**使用场景**：AI 产品、SaaS 官网、金融科技、数据管线类产品。
**反例**：长文阅读产品（玻璃与光效干扰阅读）。
**辨别要点**：如果一屏里出现两种以上颜色的光晕，或者文字浮在不确定的底色上，就已经偏离这个风格。

### 4. 书卷（Editorial）

```
风格：书卷（编辑内容）
- 表面：纯色底 + 1px 细线分隔，几乎不用阴影；圆角 2–4px 或直角
- 字体：衬线显示字体（中文宋体 / 英文 Georgia 类；Windows 回退 STSong / SimSun），标题不收紧字距，正文行高 1.7
- 强调色：全场最多 1 处（链接或 CTA 二选一）
- 留白：区块间距 ≥ 80px，标题与正文间 8–12px；宁可空，不可挤
- 气质：杂志、出版社、编辑部
```
**使用场景**：内容平台、媒体、品牌官网、文档站、博客。
**反例**：数据面板、需要一眼扫完的操作界面（衬线加大留白会拖慢扫描）。

### 5. 褐页（Sepia）

```
风格：褐页（暖褐纸面 · 旧书与档案）
- 大底：明显可辨的暖褐／羊皮纸色作为整页纸面，深褐墨色文字；
  不能只是"白卡换底色"或"按钮是棕色"
- 书页构成：页边、章节号、页码、档案条目式分隔线（点线引导）；
  章节标题与页码形成次级节奏，正文是真正可读的长文排版
- 字体：中文衬线标题（宋体 / 思源宋体类）+ 适合阅读的正文声部，
  编号与注释用等宽，形成三层声音
- 材料优先于装饰：靠纯色、边界与轻微层次表现纸面；
  纹理可选且不得影响阅读，不用渐变与光斑
- 品牌色分工：无自定义品牌色时用协调的暖褐默认色；
  自定义颜色只落在适量操作与标记上，纸面不被完全染掉
- 暗色：深褐表面 + 暖浅字，与亮色保持同一材料语言，不变成普通黑白主题
- 气质：旧书、档案、地方文献、出版与阅读
```
**使用场景**：出版与阅读产品、档案／数字人文、长文专栏、文化机构官网。
**反例**：需要"现代轻快"的消费产品（暖褐会显得沉重）。
**与书卷的区别**：书卷是当代编辑——纯色底、清晰文字结构；褐页是纸质材料本身——
整页纸面、章节页码、档案分隔。并排看时应无需看名称即可分辨。

### 6. 构色（Poster）

```
风格：构色（鲜明色块 · 海报式排版）
- 大字：标题用极大的显示字号与强字重对比，标题本身就是主视觉
- 实色色块：用少量纯色块划分区域（强调色块 / 墨色块 / 浅色块三种），
  不用多色渐变、不随意倾斜、不到处粗描边制造热闹
- 明确分区：块与块之间有硬边界与节奏性不对称，不是等分网格
- 形状语言：直角为主，阴影用无模糊的硬边错位代替柔和阴影
- 工作台：保持同一品牌形状与色块语言，但收紧标题与装饰规模，
  保证表格、表单、导航易于扫描
- 亮暗主题：两种主题下区域前景都要可读，功能色不与装饰色混淆
- 气质：文化活动、创意产品、展览、音乐节
```
**使用场景**：文化活动／创意产品的营销页、海报式落地页、需要强记忆点的品牌页。
**反例**：数据密集的管理后台（大字与色块会降低信息密度）。

### 7. 展厅（Gallery）

```
风格：展厅（作品主导 · 界面退后）
- 首屏直接是作品：第一屏就出现足够大的展示对象，不先铺通用营销标语与指标卡；
  空间名、展期、一行定位压成一条身份行
- 尺寸关系承载识别度：通过作品的比例关系（宽幅／竖幅／方形）、裁切与留白
  构成节奏，而不是把标准卡片里的图标换成图片
- 界面退后：中性展示表面 + 一个品牌行动；作品本身有颜色时，
  外围界面不再叠加大面积竞争性配色；无卡片阴影
- 工作台：以作品管理为任务——缩略图、列表／分组、筛选与状态；
  媒体是内容，不牺牲操作效率
- 占位与缺图：明确占位框、缺图状态与长标题处理，不让破图破坏版面
- 亮暗主题：保持作品比例与主体可见
- 气质：作品集、展览、项目过程、摄影与设计作品
```
**使用场景**：作品集站、展览官网、工作室官网、以图像与过程为主角的项目页。
**反例**：以文字阅读为主的内容站（作品网格会打断阅读节奏）。
**素材要求**：使用可交付、可本地引用的真实素材，或明确标注为演示的资产；
不依赖可能失效的远程图链。外部项目应替换为真实素材，**不能把案例里的演示作品当成用户资产**。

---

## 四、通用提示词模板（tokens + 风格 + 用途）

> 工具内已内置：主按钮「**复制给 AI**」会把当前 tokens 具体值 + 当前风格的全部规则
> + 必须保留 / 可自行安排的分工 + 组件 CSS + 四种用途做法 + 资源回退 + 交付自检
> 拼成一份完整输出，一键复制丢给 AI 即可。CSS 变量 / 组件 CSS / 风格提示词 / Tailwind /
> JSON 仍可在「高级导出」里单独取用。

给 AI 描述项目时，用这个三明治结构——缺了任何一层，结果都会跑偏：

```
【项目】<一句话说明产品是做什么的>
【tokens】已有一组设计 token（色彩/间距/圆角/字号/阴影），全部以 CSS 变量提供，
          只能使用这些变量，不得自造颜色
【风格】<从上方七种风格中选一种，粘贴对应提示词——它作用于整个项目的每一页>
【范围】<项目包含哪些页面：着陆页 / 工作台 / 表单 / 列表页 / 设置页 / 弹层 / 空状态…
       每页先列骨架再填内容；全站只允许这一种风格，不得另起分支>
【约束】不用渐变文字；不用 emoji 做图标（用统一描边 SVG）；阴影必须有偏移+模糊；
        一屏一个主角元素
```

### 最小接入方式

```html
<!-- 亮色：风格类挂在根节点，整站只挂一次 -->
<html lang="zh-CN" data-theme="light">
  <body class="ce-style-sepia">
    <main class="ce-landing">…</main>   <!-- 用途类：营销页 -->
    <main class="ce-app">…</main>       <!-- 用途类：工作台 -->
  </body>
</html>

<!-- 暗色：只改根节点属性，组件样式不写第二套 -->
<html lang="zh-CN" data-theme="dark">
```

四种页面用途（营销 / 工作台 / 阅读 / 展示）由统一配置管理，每种风格给出各自的适配做法。
接收方 AI 只需 tokens + 组件 CSS 两段，**不依赖工具内部的任何全局变量或选择器**。

---

## 五、五条设计约定（让页面不丑的最低标准）

1. **强调是例外**：一屏只给主按钮 / 链接 / 选中态；彩色只出现在语义位置，中性承担全部结构
2. **字体有声音**：展示字体 + 正文成对；数据用等宽 tabular 数字，别让同一字重从头走到尾
3. **组内紧、组间松**：标题组内 8px，卡片内 24px，区块间 ≥ 48px；宁可空，不可挤
4. **深度靠阴影不靠色**：阴影带偏移 + 模糊；不叠圆角胶囊、不用渐变文字
5. **禁止 AI 俗套**：图标卡平铺、眉题 eyebrow、灰底灰字、卡片套卡片——一行都不留

---

## 附一：选择风格的心法（30 秒决策）

- 产品要**显得可信/专业**（工具、数据、B2B）→ 标准
- 产品要**显得亲切/好卖**（C 端、消费）→ 暖糖
- 产品要**显得先进/清透**（AI、数据管线）→ 流光
- 产品要**显得有文化/内容**（媒体、专栏）→ 书卷
- 产品要**有纸面材料感**（出版、档案、数字人文）→ 褐页
- 产品要**大声、有记忆点**（活动、创意）→ 构色
- 产品要**让作品说话**（作品集、展览）→ 展厅
- **拿不准选什么** → 标准（不会错，也不会出彩）

---

## 附二：风格接口约定（实现层）

新增或修改风格时，只动这两处，不要在案例里散写样式：

| 位置 | 职责 |
|---|---|
| `design-profiles.js` → `DESIGN_PROFILES[id].world` | 世界变量：圆角 / 线宽 / 状态点 / 字体声部 / 阴影 / 材料语义层（`--ce-page`、`--ce-rule`、`--ce-ink`…） |
| `design-profiles.js` → `DESIGN_PROFILES[id].css` | 作用域样式：只写 `.ce-style-<id>` 下的规则，不写全局选择器 |
| `design-profiles.js` → `prompt` / `rules` / `purposes` | 导出用文案：必须保留的特征、规避项、每种页面用途的做法 |

案例构建函数（`color-engine-demo.js`）只负责内容与编排；缩略图、比较样张、导出
全部从这份注册表派生，改一处即三处生效。

新增风格时必须同时提供：**完整着陆页 + 完整工作台**，且包含足以验证风格的标题、正文、
导航、按钮、输入、内容列表／卡片与状态；只有色板或孤立按钮不算一个风格。

### 已移除与回退

| 旧标识 | 现状 |
|---|---|
| `spectrum` 光谱 | 已从生成风格阵容移除；旧单份状态中的该值自动映射到 `standard`，打开页面不报错 |
| `brutalism` / `brutal` | 映射到 `poster` |
| `galleryWhite` | 映射到 `gallery` |
| `warm` | 映射到 `soft` |
| 空值 / `null` / 未知值 | 回退 `standard` |
